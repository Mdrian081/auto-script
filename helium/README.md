# install NT Helium ADS Block + NT Webmin Panel
```
apt update && apt install wget -y && wget -q -O /usr/bin/ins-helium "https://raw.githubusercontent.com/Mdrian081/auto-script/master/helium/ins-helium.sh" && chmod +x /usr/bin/ins-helium && ins-helium
```
